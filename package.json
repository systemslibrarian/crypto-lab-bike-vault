{
  "name": "crypto-lab-bike-vault",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive browser demo of BIKE (Bit Flipping Key Encapsulation) — a code-based post-quantum KEM",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
